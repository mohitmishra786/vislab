---
title: Inode tree
description: Inode tree systems visualization widget.
---

import { InodeTree } from "@vislab/react";

Inode tree systems visualization widget.

**Maturity:** `beta` — works end-to-end; pedagogy docs thinner than flagships.

## Live demo

<figure>
  <InodeTree client:visible />
  <figcaption>Inode tree interactive simulation.</figcaption>
</figure>

## Props

| Prop | Type | Optional | Description |
| ---- | ---- | -------- | ----------- |
| `themeName` | `string` | yes | Theme id, e.g. "dark-premium" |


## Snippets

### MDX / Astro

```astro
---
import { InodeTree } from "@vislab/react";
---
<figure>
  <InodeTree client:visible />
  <figcaption>Inode tree</figcaption>
</figure>
```

### HTML

```html
<div data-vislab="InodeTree" style="min-height:320px;width:100%"></div>
```

### Jekyll

```liquid
{% include vislab.html id="inode-tree" component="InodeTree" %}
```

## Accessibility & SEO

Canvas is visual-only. Use a figcaption and surrounding prose. Controls include SimClock (pause / 0.1× / 1× / 10×) and a **Static** SVG export for a text+snapshot twin.

## Registry

- **id:** `inode-tree`
- **globalName:** `InodeTree`
- **category:** `os`
- **custom element:** `<vislab-inode-tree>`
