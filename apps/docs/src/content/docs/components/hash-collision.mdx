---
title: Hash collisions
description: Hash collisions systems visualization widget.
---

import { HashCollision } from "@vislab/react";

Hash collisions systems visualization widget.

**Maturity:** `beta` — works end-to-end; pedagogy docs thinner than flagships.

## Live demo

<figure>
  <HashCollision client:visible />
  <figcaption>Hash collisions interactive simulation.</figcaption>
</figure>

## Props

| Prop | Type | Optional | Description |
| ---- | ---- | -------- | ----------- |
| `themeName` | `string` | yes | Theme id, e.g. "dark-premium" |


## Snippets

### MDX / Astro

```astro
---
import { HashCollision } from "@vislab/react";
---
<figure>
  <HashCollision client:visible />
  <figcaption>Hash collisions</figcaption>
</figure>
```

### HTML

```html
<div data-vislab="HashCollision" style="min-height:320px;width:100%"></div>
```

### Jekyll

```liquid
{% include vislab.html id="hash-collision" component="HashCollision" %}
```

## Accessibility & SEO

Canvas is visual-only. Use a figcaption and surrounding prose. Controls include SimClock (pause / 0.1× / 1× / 10×) and a **Static** SVG export for a text+snapshot twin.

## Registry

- **id:** `hash-collision`
- **globalName:** `HashCollision`
- **category:** `algorithms`
- **custom element:** `<vislab-hash-collision>`
